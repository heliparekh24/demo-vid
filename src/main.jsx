import React from "react";
import ReactDOM from "react-dom/client";
import TomatoMVP from "./TomatoMVP";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TomatoMVP />
  </React.StrictMode>
);
